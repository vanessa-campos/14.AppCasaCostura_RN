export default class Venda
{
    constructor(Nome, Valor, Quantidade, ValorTotal, Data)
    {
        this.Nome = Nome;
        this.Valor = Valor;
        this.Quantidade = Quantidade;
        this.ValorTotal = ValorTotal;
        this.Data = Data;
    }
}